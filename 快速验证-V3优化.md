# 快速验证 V3 优化效果

## 验证目标

验证豆瓣详情页图片（`/view/photo/l/` 格式）是否能正确升级到 `/view/photo/raw/` 最高清版本。

## 测试用例

### 用例 1: 生命树海报（用户提供的案例）

**测试URL**: https://movie.douban.com/photos/photo/2929311086/

**期望结果**:
- 原始URL: `https://img9.doubanio.com/view/photo/l/public/p2929311086.webp`
- 升级后URL: `https://img9.doubanio.com/view/photo/raw/public/p2929311086.webp`
- 图片尺寸: 1280x2276 像素
- 文件大小: 800KB - 2MB

## 快速验证步骤

### 方法 1: 使用测试脚本（推荐）

```bash
cd /Users/prospect/Documents/code/tcxy/xygj/giraffe-material-auto

# 确保服务正在运行
# 如果没有运行，先启动：
# java -jar target/giraffe-material-auto-*.jar

# 运行测试脚本
./test_douban_hd.sh
```

脚本会自动：
1. 发送下载请求
2. 显示下载结果
3. 检查图片尺寸和文件大小
4. 判断是否达到高清标准

### 方法 2: 手动测试

#### 步骤 1: 发送下载请求

```bash
curl -X POST http://localhost:8080/api/v1/download/images \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://movie.douban.com/photos/photo/2929311086/",
    "targetDir": "test_v3_hd",
    "useTimestamp": false
  }' | jq '.'
```

#### 步骤 2: 查看响应

期望看到：
```json
{
  "success": true,
  "message": "下载完成",
  "totalCount": 1,
  "successCount": 1,
  "failCount": 0,
  "savePath": "downloads/douban_movie/test_v3_hd",
  "duration": 2000
}
```

#### 步骤 3: 检查日志

```bash
tail -f logs/giraffe-material-auto.log
```

期望看到：
```
升级图片URL为高清: https://img9.doubanio.com/view/photo/l/public/p2929311086.webp 
  -> https://img9.doubanio.com/view/photo/raw/public/p2929311086.webp
成功下载: https://img9.doubanio.com/view/photo/raw/public/p2929311086.webp
```

#### 步骤 4: 验证图片

```bash
# 查看文件大小
ls -lh downloads/douban_movie/test_v3_hd/

# 查看图片尺寸（macOS）
sips -g pixelWidth -g pixelHeight downloads/douban_movie/test_v3_hd/*.webp

# 查看图片尺寸（Linux）
identify downloads/douban_movie/test_v3_hd/*.webp
```

期望输出：
```
pixelWidth: 1280
pixelHeight: 2276
```

### 方法 3: 对比测试

同时下载优化前后的版本进行对比：

```bash
# 手动下载 large 版本（优化前）
curl -o /tmp/test_large.webp \
  "https://img9.doubanio.com/view/photo/l/public/p2929311086.webp"

# 手动下载 raw 版本（优化后）
curl -o /tmp/test_raw.webp \
  "https://img9.doubanio.com/view/photo/raw/public/p2929311086.webp"

# 对比文件大小
ls -lh /tmp/test_*.webp

# 对比图片尺寸
sips -g pixelWidth -g pixelHeight /tmp/test_*.webp
```

期望看到明显差异：
```
test_large.webp:  ~200KB,  1080x1920
test_raw.webp:    ~1.5MB,  1280x2276  ✅
```

## 验证清单

- [ ] 编译成功（`mvn clean compile`）
- [ ] 服务启动成功
- [ ] API 请求返回 success: true
- [ ] 日志中出现 "升级图片URL为高清"
- [ ] 下载的图片文件大小 > 500KB
- [ ] 图片尺寸为 1280x2276 或接近
- [ ] 放大图片后细节清晰

## 常见问题排查

### 问题 1: 日志中没有 "升级图片URL为高清"

**可能原因**:
- 代码没有重新编译
- 服务没有重启

**解决方法**:
```bash
# 重新编译
mvn clean package

# 重启服务
pkill -f giraffe-material-auto
java -jar target/giraffe-material-auto-*.jar
```

### 问题 2: 图片尺寸还是 1080x1920

**可能原因**:
- URL 没有正确升级
- 豆瓣返回了重定向

**解决方法**:
- 查看日志确认 URL 是否包含 `/raw/`
- 检查网络连接是否正常

### 问题 3: 下载失败或超时

**可能原因**:
- 网络问题
- 豆瓣限流

**解决方法**:
- 增加超时配置
- 增加重试次数
- 稍后再试

## 与参考图片对比

用户提供的参考图片：
- 路径: `/Users/prospect/Downloads/漫云创/素材/生命树/海报图/1.jpg`
- 这是通过微信下载的高清版本

验证时应该：
1. 将 API 下载的图片与参考图片放在一起
2. 对比文件大小（应该接近）
3. 对比图片尺寸（应该相同或接近）
4. 放大后对比细节清晰度

## 成功标准

✅ **验证通过的标准**:
1. 日志中明确显示 URL 从 `/l/` 升级到 `/raw/`
2. 下载的图片文件大小 >= 800KB
3. 图片尺寸 >= 1200x2000 像素
4. 放大后细节清晰，无明显模糊
5. 与用户提供的参考图片清晰度相当

## 下一步

验证通过后：
1. 可以用于生产环境
2. 批量下载其他豆瓣图片
3. 根据需要调整配置参数

验证失败：
1. 查看日志找出问题
2. 参考 [豆瓣高清图优化说明-V3.md](./豆瓣高清图优化说明-V3.md)
3. 联系开发团队

---

**更新日期**: 2026-01-27

