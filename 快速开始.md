# ⚡ 3分钟快速配置豆瓣Cookie

## 🎯 目标：让系统能够访问豆瓣页面

---

## 📝 第1步：获取Cookie（2分钟）

### 🖥️ 在浏览器中操作：

1. **打开Chrome浏览器**

2. **访问这个链接**：
   ```
   https://movie.douban.com/photos/photo/2925525013/
   ```

3. **如果看到"点我继续浏览"，点击它**

4. **按键盘上的 `F12`** （Mac用户按 `Command + Option + I`）
   - 会打开一个开发者工具窗口

5. **点击顶部的 `Network` 标签**

6. **按 `F5` 刷新页面**

7. **在列表中点击第一个项目**（通常是数字开头的）

8. **在右侧向下滚动，找到 `Request Headers`**

9. **找到 `Cookie:` 这一行，右键点击它的值 → 选择 "Copy value"**
   - Cookie值类似这样：`bid=xxx; dbcl2=xxx; ck=xxx; ...`
   - 很长，没关系，全部复制

---

## ✏️ 第2步：配置Cookie（30秒）

1. **打开文件**：
   ```
   /Users/prospect/Documents/code/tcxy/xygj/giraffe-material-auto/src/main/resources/application.yml
   ```

2. **找到第32行**左右，看到：
   ```yaml
   douban-cookie: ""
   ```

3. **把刚才复制的Cookie粘贴到双引号中间**：
   ```yaml
   douban-cookie: "bid=xxx; dbcl2=xxx; ck=xxx; _pk_id=xxx; ..."
   ```
   
   ⚠️ **注意**：
   - 整个Cookie用**一对双引号**包裹
   - 如果Cookie中有双引号 `"`，删掉它们，或者在前面加 `\`

4. **保存文件**（`Command + S` 或 `Ctrl + S`）

---

## 🧪 第3步：测试Cookie是否有效（30秒）

在终端执行：

```bash
cd /Users/prospect/Documents/code/tcxy/xygj/giraffe-material-auto
./test_cookie.sh test
```

### 如果看到：
✅ **"测试成功！Cookie有效"** → 进入第4步  
❌ **"测试失败：被豆瓣拦截"** → 返回第1步重新获取Cookie

---

## 🚀 第4步：重启服务并测试（30秒）

```bash
# 停止旧服务（按 Ctrl+C）
# 然后重启
mvn spring-boot:run
```

等服务启动后，发送请求：

```json
{
    "url": "https://movie.douban.com/photos/photo/2925525013/",
    "targetDir": "douban_movie",
    "convertToJpeg": true,
    "crawlAllPages": true
}
```

---

## 🎉 成功标志

日志中会看到：

```
✅ 使用豆瓣Cookie进行请求
📄 页面标题: 生命树 剧照
✓ 提取到高清图: https://img3.doubanio.com/view/photo/raw/public/p2925525013.jpg
```

然后开始批量下载 252 张高清图片！

---

## 🆘 还是不行？

### 检查清单：

- [ ] Cookie是否正确复制？（包含 `bid=`、`dbcl2=`、`ck=` 等）
- [ ] Cookie是否用双引号包裹？
- [ ] 是否保存了 `application.yml` 文件？
- [ ] 是否重启了服务？
- [ ] 运行 `./test_cookie.sh test` 是否显示"测试成功"？

### 如果以上都检查了还不行：

请复制以下信息给我：

1. **运行测试脚本的输出**：
   ```bash
   ./test_cookie.sh test
   ```

2. **Cookie的前50个字符**（不要发完整Cookie）：
   ```bash
   grep "douban-cookie" src/main/resources/application.yml | cut -c 1-80
   ```

3. **最新的错误日志**：
   ```bash
   tail -50 logs/giraffe-material-auto.log | grep -E "(页面标题|拦截|Cookie)"
   ```

---

## 💡 提示

- Cookie通常几天到几周后过期，届时重新获取即可
- 不要把Cookie分享给别人（包含隐私信息）
- 如果换了浏览器或清除了浏览器数据，需要重新获取
